// In one file (myModule.js)
export function sayHello() {
  console.log("Hello changes in the file are tey change!");
}

export function add(a, b) {
  return a + b;
}

export function subtract(a, b) {
  return a - b;
}

// Modules in JS:
// Definition: Modules in JavaScript allow you to split your code
// into multiple files, making it easier to manage and organize.
// Each file is a module that can export and import code.
